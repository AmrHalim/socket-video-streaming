var config = {}
config.port = 9999;
config.public_directory = 'public';
config.privatekey_file = 'video_streaming.key';
config.certificate_file = 'video_streaming.crt';
config.certrequest_file = 'video_streaming.csr';

module.exports = config;
